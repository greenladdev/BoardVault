PORT=3001
DB_PATH=./boardvault.db

# Optional: auto-login to BGG on startup so search works without manually
# connecting in the UI. Leave blank to connect via the in-app login form.
BGG_USERNAME=
BGG_PASSWORD=
